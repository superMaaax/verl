# Format checks enforced on CI:
# 1. Comments must appear above each field.
# 2. There must be a blank line between each field.
# 3. Inline comments (after a field on the same line) are not allowed.
# 4. Indentation level is respected for nested fields.

# <folder_name>@<field_name>.<field_name>: <yaml_file_name>

defaults:
  - model@model: hf_model
  - engine@engine: fsdp
  - optim@optim: fsdp
  - profiler@profiler: profiler
  - _self_

data:
  train_batch_size: 256 # global batch size
  micro_batch_size_per_gpu: 4  # this is also val batch size
  max_token_len_per_gpu: 8192
  use_dynamic_bsz: True
  train_files: ~/data/gsm8k/train.parquet
  val_files: null
  loss_val_files: null
  generation_eval_files: null
  train_max_samples: -1  # set to -1 to use full dataset
  val_max_samples: -1  # set to -1 to use full dataset
  # Multi-turn settings
  messages_key: messages  # Key for messages list in multi-turn mode
  generation_eval_prompt_key: prompt
  generation_eval_batch_size: null
  generation_eval_max_prompt_length: 2048
  generation_eval_filter_overlong_prompts: False
  generation_eval_truncation: error
  generation_eval_apply_chat_template_kwargs:
    enable_thinking: null
  tools_key: tools  # Key for tools list in multi-turn mode
  enable_thinking_key: enable_thinking  # Whether to enable thinking in multi-turn mode
  enable_thinking_default: none         # The default value when enable_thinking_key is not present in the dataset
  pad_mode: no_padding
  # for right padding
  max_length: 1024
  truncation: error
  balance_dp_token: False # to be implement
  custom_cls:
    path: null
    name: null
  use_shm: False
  apply_chat_template_kwargs: {}
  num_workers: 8

  # MultiTurnSFTDataset apply_chat_template to each turn separately and concat `input_ids`
  # as a whole sequence, which may not equal to apply_chat_template to whole messages at once. 
  # For example, Qwen Thinking series models add <think></think> tags to last turn, please check
  # your tokenizer chat template settings.
  # Set to True to ignore input_ids mismatch and use the concatenated input_ids as the final input_ids.
  ignore_input_ids_mismatch: False

# Checkpoint configuration
checkpoint:
  _target_: verl.trainer.config.CheckpointConfig
  # What to include in saved checkpoints
  # with 'hf_model' you can save whole model as hf format, now only use sharded model checkpoint to save space
  save_contents: ["model", "optimizer", "extra"]

  # For more flexibility, you can specify the contents to load from the checkpoint.
  load_contents: ${checkpoint.save_contents}
  # Mbridge config extension.
  mbridge_config: {}

sparse_update:
  # Whether to enable sparse SFT parameter updates.
  enabled: false

  # Mask mode: safe_svd_lowmag, non_principal, low_magnitude, principal, random_same_density, or wanda_top.
  mode: safe_svd_lowmag

  # Optional path to load a prebuilt mask file.
  mask_path: null

  # Optional path to save masks generated at initialization.
  save_mask_path: null

  # Build masks from initial model weights before training starts.
  build_mask_on_init: false

  # Rank for SVD reconstruction of pretrained linear weights.
  rank_k: 128

  # Fraction of rank-k reconstruction entries considered principal.
  alpha_princ: 0.5

  # Fraction of original weight entries considered low-magnitude.
  alpha_low: 0.5

  # Linear module suffixes to mask.
  target_modules:
    - q_proj
    - k_proj
    - v_proj
    - o_proj
    - gate_proj
    - up_proj
    - down_proj

  # Parameter name keywords to skip.
  exclude_keywords:
    - embed
    - lm_head
    - norm
    - layernorm
    - rmsnorm

  # Whether bias tensors may be masked.
  apply_to_bias: false

  # Restore frozen entries after optimizer.step.
  restore_frozen_after_step: true

  # Actually prune frozen entries by setting them to zero and keeping them zero.
  zero_frozen_params: false

  # Zero optimizer state entries corresponding to frozen weights.
  mask_optimizer_state: true

  # Periodically verify frozen entries remain unchanged.
  verify_frozen_weights: false

  # Verification interval in optimizer steps.
  verification_interval: 10

  # Absolute tolerance for frozen-weight verification.
  verification_tolerance: 1.0e-6

  # Use a random mask with layerwise safe-mask density.
  random_mask_same_density: false

  # Build/log masks but do not enforce them.
  dry_run_log_only: false

trainer:
  default_local_dir: checkpoints/${trainer.project_name}/${trainer.experiment_name}
  default_hdfs_dir: null
  project_name: gsm8k-sft
  experiment_name: test
  total_epochs: 4
  total_training_steps: null
  logger: [ 'console', 'wandb' ]
  seed: 1
  save_freq: -1
  test_freq: -1
  loss_test_freq: null
  max_ckpt_to_keep: null  # Maximum number of checkpoints to keep, set to null to keep all
  eval_method: loss
  val_before_train: False
  generation_eval:
    backend: hf
    max_new_tokens: 2048
    do_sample: False
    temperature: 1.0
    top_p: 1.0
    top_k: null
    n: 1
    test_freq: null
    dtype: null
    vllm_gpu_memory_utilization: 0.2
    vllm_host_ip: 127.0.0.1
    vllm_enforce_eager: True
    vllm_sync_weights: True
    vllm_enable_multiprocessing: False
    reward_kwargs: {}

  # Resume mode: "auto", "disable", or "resume_path"
  # "auto": resume from last checkpoint if available
  # "disable": start from scratch
  # "resume_path": resume from a user-defined path
  resume_mode: auto

  # Path to resume training from (used when resume_mode is "resume_path" or "auto")
  resume_from_path: null  
  device: cuda

  nnodes: 1
  n_gpus_per_node: 1

  profile_interval: [-1, -1]
